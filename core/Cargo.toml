[package]
name = "panorama-core"
version = "0.1.0"
edition = "2021"

[dependencies]
entity = { path = "entity" }
migration = { path = "migration" }

prost = "0.13.3"
tonic = "0.12.3"

sea-orm = { version = "1.1.1", features = [
  "sqlx-sqlite",
  "runtime-tokio-rustls",
  "macros",
] }

[build-dependencies]
tonic-build = "0.12.3"
